# URL del backend (sin trailing slash). En dev no es estrictamente necesario
# porque Vite hace proxy de /api hacia esta URL desde vite.config.ts.
# En prod se construye en el build y debe apuntar a la URL pública del API.
VITE_API_URL=http://localhost:8000
